# Battle Simulator - Requirements
# Install these packages before running the simulator

pandas>=1.5.0
requests>=2.28.0

# Note: tkinter comes with Python by default on most systems
# If tkinter is missing, install python3-tk on Linux:
# sudo apt-get install python3-tk  (Ubuntu/Debian)
# sudo yum install tkinter         (CentOS/RHEL)
# brew install python-tk           (macOS with Homebrew)
